// The Schedule page (src/pages/Schedule.tsx) renders no static text of its own —
// its only content is the embedded Cal.com widget, which supplies its own UI copy.
// Kept for structural consistency with the other pages.
export const schedule = {
  calLink: 'raindfwphotos/request-availability',
}
