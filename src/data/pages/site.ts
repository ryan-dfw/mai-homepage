// Site-wide, non-page-specific text (document metadata).
// NOTE: index.html is static markup and does not currently read this file —
// wiring it up would mean setting document.title from React (e.g. in main.tsx).
// Kept here so the string is still tracked as an editable text source.
export const site = {
  title: 'Mai Melancholia',
}
