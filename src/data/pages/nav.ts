// Copy for the site-wide navigation header (appears on every page).
// Link labels/routes and menu aria-labels are nav config, not copy — see ../navLinks.ts.
export const nav = {
  brand: 'Lorem Ipsum',
}
